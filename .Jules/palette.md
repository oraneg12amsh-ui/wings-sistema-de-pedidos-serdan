## 2026-03-28 - Adding explicit ARIA labels in JavaScript templates
**Learning:** When generating interactive components via JavaScript string interpolation, it's crucial to include dynamic `aria-label`s (e.g., `aria-label="Agregar ${item.name}"`) to provide context for screen readers.
**Action:** Always inspect JS template literals for icon-only buttons when doing accessibility audits.
